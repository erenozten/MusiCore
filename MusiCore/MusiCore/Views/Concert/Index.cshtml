@model MusiCore.ViewModels.ConcertsViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*@Html.Partial("PartialViews/_PartialViewForCreateTextTop")*@
@await Html.PartialAsync("PartialViews/_PartialViewForCreateTextTop")


<ul id="ulId" class="gigs">
    @foreach (var item in Model.UpcomingConcerts)
    {
        // <li> <div id="MainDiv"> <div id="followOrUnfollowDiv"> <div id="followDiv"> <button id="followOrUnfollowButton">
        <li>
            <div id="MainDiv" class="MainDiv" style=" height: 65px;">
                <div class="firstDiv date date i-display-inline-block">
                    <div class="month">
                        @item.DateTime.ToString("MMM")
                    </div>
                    <div class="day">
                        @item.DateTime.ToString("d ")
                    </div>
                </div>
                <div id="ArtistNameAndGenreDiv" class="secondDiv ArtistNameAndGenreDiv i-display-inline-block">
                    <div id="artistNameDiv" class="artist artistNameDiv gigHub-artistLabel" val="@item.Artist.Name" style="">@item.Artist.Name</div>
                    <div>
                        <div class="gigHub-genreLabel" style="">
                            @item.Genre.Name
                        </div>
                    </div>
                </div>
                <div id="followOrUnfollowDiv" class="thirdDiv i-display-inline-block" style="position: absolute; left: 206px; top: 1px;">
                    <div id="followDiv" class="followDiv">
                        @*btn-sm çıkarıldı   btn-sm-for-follow eklendi*@
                        <button id="followOrUnfollowButton" onclick="followOrUnfollow(this)"
                                class="followOrUnfollowButton btn btn-link js-toggle-follow gigHub-followLabel gigHub-followLabelNotFollowing followOrUnfollowButton btn-sm-for-follow" style="" data-gig-id=@item.Id value="Takip et">
                            Takip Et
                        </button>
                    </div>
                    <div id="goingDiv">
                        <button onclick="attendToConcertFunc(this)" id="goingButtonId" data-gig-id=@item.Id data-artist-id=@item.Artist.Id 
                                value="Katıl" style="position: absolute; left: 1px; top: 29px; width: 109px;" 
                                class="gigHub-goingButton btn btn-default btn-sm pull-right btnGoing gigHub-goingButtonPassive" data-artist-id="d3aa7f47-959e-414a-a467-2da769a92cc6">
                            Katıl
                        </button>
                    </div>
                </div>
            </div>
        </li>
    }
</ul>

<ul id="ornekUl">
    <li>
        <div>
            <div>
                <div class="artistNameDiv" id="artistNameDiv">art1s</div>
            </div>
            <div>
                gereksiz
            </div>
        </div>
    </li>
    <li>
        <div>
            <div>
                <div id="artistNameDiv">art2</div>
            </div>
            <div>
                gereksiz
            </div>
        </div>
    </li>
</ul>

@section scripts
{
    <script>

        function attendToConcertFunc(basilanButton) {
            //alertify.myAlert("Browser dialogs made easy!");
            //Console.log("s232323");
            debugger; /* 32314*/

            var goingDiv = $(basilanButton).parent("#goingDiv");
            $(goingDiv).append("<div id='ball1id' class='ball1'></div>");
            debugger; /* 32314*/

            $(basilanButton).prop("disabled", true);
            setTimeout(function () {

                var doIWannaGoInJQuery;
                var gigIdFromButton = $(basilanButton).attr('data-gig-id');
                var artistIdInJQuery = $(basilanButton).attr('data-artist-id');

                debugger; /* 32314*/
                if (basilanButton.value == "Katılıyorum!") {
                    doIWannaGoInJQuery = false;
                }
                if (basilanButton.value == "Katıl") {
                    doIWannaGoInJQuery = true;
                    debugger; /* 323145*/

                }
                //debugger;

                $.ajax(
                    {
                        type: "POST",
                        url: "Json/Attend",
                        datatype: "json", //bunu stackover..dan aldım
                        //data: JSON.stringify({
                        //    //bu data kısmını server'a yolluyoruz. Server'ın beklediği parametreler solda yazılı (GigId, doIWannaGoInJQuery). Sağdakiler de html'den çektiğimiz değerler (firstMidName, lastName, enrollmentDate. Yani html'deki değereleri server'a değişkenler üzerinden yolluyoruz.
                        //    gigIdInActionResult: gigIdFromButton,
                        //    doIWannaGoInActionResult: doIWannaGoInJQuery,
                        //    artistIdInActionResult: artistIdInJQuery
                        //}),
                        data: {
                            gigIdInActionResult: gigIdFromButton,
                            doIWannaGoInActionResult: doIWannaGoInJQuery,
                            //artistIdInActionResult: artistIdInJQuery
                        },

                        success: function (data) {

                            $(basilanButton).prop("disabled", false);

                            //setTimeout(attendToConcertFunc,
                            //    1000);
                            //güya işlem yapılıyor... Loading animasyonunu görmek için de bu delay'ı koymak gerekiyor. Real time app gibi yani...
                            $("#ball1id").remove(); //Loading animasyonunu delay bittikten sonra kaldırmış olduk.

                            //debugger;

                            if (data.resultOfGoing == true) {
                                $(basilanButton).text('Katılıyorum!');
                                $(basilanButton).attr("value", "Katılıyorum!");
                                $(basilanButton).addClass("gigHub-goingButtonActive");

                                //debugger;
                                //alertify.success("<span><font size='5'>✓</font>    Katılımınız kaydedildi!</span><br/>Konser yeri: " + data.concertVenue + "</span > <br />Sanatçı: " + data.concertArtistName);
                                alertify.success(
                                    "<span><font size='5'>✓</font>    Katılımınız başarıyla kaydedildi!</span>");
                            }

                            if (data.resultOfGoing == false) {

                                if (data.isItMyGig) {
                                    $("#ball1id").remove();

                                    //alert("Bu konseri siz veriyorsunuz");
                                    alertify.success("<span><font size='5'>✓</font>    Bu konser sizin!</span>");
                                    //debugger;
                                    return;
                                }

                                $(basilanButton).text('Katıl');
                                $(basilanButton).attr("value", "Katıl");
                                $(basilanButton).removeClass("gigHub-goingButtonActive");
                                
                                alertify.success(
                                    "<span><font size='5'>✓</font>    Katılımınız başarıyla silindi!</span>");

                                var datadakiVeri1 = data.kafadanAtmaText;
                                var datadakiVeri3 = data.resultOfGoing;
                                //debugger;
                            }
                        },

                        error: function (data) {
                            $(basilanButton)
                                .prop("disabled",
                                    false); // işlem fail olmuşsa butonu disable yapmanın bi mantığı yok, o yüzden disabled özelliğini burada iptal ettik.
                            //debugger;
                        }
                    });
            },
                1000);
        };


        //Button'un onclick event'ında bu fonksiyon çalışıyor, parametre olara ise button'ı veriyoruz this keyword'ü ile (onlick="followOrUnfollow(this)").
        function followOrUnfollow(basilanButton) {

            //button'un bağlı olduğu div'i seçmek için sürekli DOM'a gitmek gerek. Bunu önlemek için followOrUnfollow değişkenine div'i attık. Artık dom'a gitmeyeceğiz hep.
            var followOrUnfollowDiv = $(basilanButton).parent("#followOrUnfollowDiv");

            debugger; //top
            //click yapılır yapılmaz dönen loading topunu oluşturuyoruz
            $(followOrUnfollowDiv).append("<div id='ball1id' class='ball1'></div>");
            $(basilanButton).append("<div id='ball1id' class='ball1'></div>");
            debugger; //top

            //$(basilanButton).find("#goingDiv").append("<div id='ball1id' class='ball1'></div>");

            //$(goingDiv).append("<div id='ball1id' class='ball1'></div>");

            //click esnasında button'ı disable ediyoruz. Böylece kullanıcı tekrar tıklayabilmek için işlemin bitmesini bekleyecek.
            $(basilanButton).prop("disabled", true);
            setTimeout(function () {

                var wannaFollow;
                var wannaUnfollow;
                var gigIdFromButton = $(basilanButton).attr('data-gig-id');

                if (basilanButton.value == "Takip et") {
                    wannaFollow = true;
                }
                if (basilanButton.value == "Takiptesin!") {
                    wannaUnfollow = true;
                    //font-weight: bold;
                    //color: #e060d6;
                }
                //debugger; /* 1234*/

                $.ajax(
                    {
                        type: "POST",
                        url: "Json/FollowOrUnfollow",
                        datatype: "json",
                        data: {
                            gigIdInJsonResult: gigIdFromButton,
                            wannaFollowInJsonResult: wannaFollow,
                            wannaUnfollowInJsonResult: wannaUnfollow,
                            //artistIdInActionResult: artistIdInJQuery,
                            //followedOrUnfollowedUserId
                        },

                        success: function (data) {

                            $(basilanButton).prop("disabled", false);

                            //setTimeout(followOrUnfollow, 1000); //güya işlem yapılıyor... Loading animasyonunu görmek için de bu delay'ı koymak gerekiyor. Real time app gibi yani...
                            $("#ball1id").remove(); //Loading animasyonunu delay bittikten sonra kaldırmış olduk.

                            //debugger; /*1*/

                            if (data.resultOfFollowing == true) {
                                //debugger; /*32323*/

                                $(basilanButton).text('Takiptesin!');
                                $(basilanButton).attr("value", "Takiptesin!");
                                $(basilanButton).addClass("gigHub-followLabelFollowing");
                                $(basilanButton).removeClass("gigHub-followLabelNotFollowing");

                                //debugger; /*2*/

                                // #ulId -> bizim <ul> elementimizin id'si. Bu ul'un içindeki <li> leri .each ile seçiyoruz
                                $('#ulId li').each(function (index, value) {

                                    var w22e = $(value).find('div#artistNameDiv').text();

                                    if (w22e == data.followedOrUnfollowedArtistName) {
                                        debugger;
                                        $(this).find(".followOrUnfollowButton").text('Takiptesin!');
                                        $(this).find(".followOrUnfollowButton").attr("value", "Takiptesin!");
                                        $(this).find(".followOrUnfollowButton").addClass("gigHub-followLabelFollowing");
                                        $(this).find(".followOrUnfollowButton").removeClass("gigHub-followLabelNotFollowing");
                                        //$(this).addClass("gigHub-followLabelFollowing");
                                        //$(this).removeClass("gigHub-followLabelNotFollowing");
                                    }
                                    debugger;
                                });

                                //alertify.success("<span><font size='5'>✓</font>    Katılımınız kaydedildi!</span><br/>Konser yeri: " + data.concertVenue + "</span > <br />Sanatçı: " + data.concertArtistName);
                                alertify.success(
                                    "<span><font size='5'>✓</font>     Şu sanatçıyı takip ediyorsunuz: </span>" +
                                    data.followedOrUnfollowedArtistName);
                            }

                            if (data.resultOfUnfollowing == true) {
                                debugger; /*3*/

                                if (data.isItMyGig) {
                                    $("#ball1id").remove(); 

                                    //alert("Bu konseri siz veriyorsunuz");
                                    alertify.success("<span><font size='5'>✓</font>    Bu konser sizin!</span>");
                                    return;
                                }

                                $(basilanButton).text('Takip et');
                                $(basilanButton).attr("value", "Takip et");

                                debugger; /*3*/

                                //$(basilanButton).removeClass("gigHub-goingButtonActive");
                                //alertify.success("<span><font size='5'>✓</font>    Katılımınız başarıyla silindi!</span>");
                                alertify.success(
                                    "<span><font size='5'>✓</font>     Şu sanatçıyı takibi bıraktınız: </span>" +
                                    data.followedOrUnfollowedArtistName);
                                debugger; /*5*/

                                $('#ulId li').each(function (index, value) {

                                    console.log($(this)); //'this' ile 'value' galiba aynı şeye işaret ediyor. Görünene göre öyle (sonuç incelendi). Fark var mı bilmiyorum.
                                    console.log($(value)); //'this' ile 'value' galiba aynı şeye işaret ediyor. Görünene göre öyle (sonuç incelendi). Fark var mı bilmiyorum.

                                    $(this).find(".artistNameDiv").val();
                                    $(value).find(".artistNameDiv").val();

                                    //var qwez = $(this).find(".artistNameDiv").val();
                                    //var wqewewe = $(value).find(".artistNameDiv").val();

                                    //var qwezcxc = $(this).find("#artistNameDiv").val();
                                    //var wqewecxcwwe = $(value).find("#artistNameDiv").val();

                                    //var qweqwezdsdcxc = $(this).find('div.artistNameDiv').val();
                                    //var wqeweescxcwwe = $(value).find('div#artistNameDiv').val();

                                    var w22e = $(value).find('div#artistNameDiv').text();

                                    if (w22e == data.followedOrUnfollowedArtistName) {
                                        debugger;
                                        $(this).find(".followOrUnfollowButton").text('Takip et');
                                        $(this).find(".followOrUnfollowButton").attr("value", "Takip et");

                                        //$(basilanButton).addClass("gigHub-followLabelFollowing"); 
                                        //$(basilanButton).removeClass("gigHub-followLabelNotFollowing");

                                        $(this).find(".followOrUnfollowButton").removeClass("gigHub-followLabelFollowing"); 
                                        $(this).find(".followOrUnfollowButton").addClass("gigHub-followLabelNotFollowing");
                                        debugger;
                                    }
                                });

                            }
                        },

                        error: function (data) {
                            $(basilanButton)
                                .prop("disabled",
                                    false); // işlem fail olmuşsa butonu disable yapmanın bi mantığı yok, o yüzden disabled özelliğini burada iptal ettik.
                            debugger; /*6*/
                        }
                    });
            },
                800);
        };

                        // Bir michael jackson takip edildiğinde jsonresult'a dön. Takip etme isteği gelmişse, takip=true script koduna git, orada tüm butonları değiştir.
                        // Tablodaki tüm <li> itemleri içinde bulunan artist'i barındıran <div> ler içinden ArtistName seçilmeli.
                        // Bu ArtistName değeri, "Michael Jackson" a eşitse, takip et butonu takiptesin e dönüşmeli.
    </script>
}